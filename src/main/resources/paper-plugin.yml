name: TheTowers
version: '${version}'
authors:
  - ErPiX
description: "A core plugin for the Exvoid Network"
prefix: "TheTowers"
main: dev.erpix.thetowers.TheTowersBootstrap
api-version: '1.21.5'
dependencies:
  server:
    TAB:
      load: BEFORE
      required: true
      join-classpath: true